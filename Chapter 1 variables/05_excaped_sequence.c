#include<stdio.h>

int main(){
    int a;
    char ch = 'a';
    ch = 'b';
//  These are escaped sequence \n \t \\ \' \" 
/*multi line 
comment */
//  crtl + /   -- commenting and un commenting key
// shift + alt + down arrow (copies the whole line and paste it down )
//alt + arrow up or down keys can interchange lines 
// for long variables names use first letter of the variable and press enter 
// anything inside if statement is connsider true unless correct relations operators are used
// example
// int a=10;
// if (a=11)
// this if is considered true
// also { } can be skipped after if , else if and else if only one is written after it
}